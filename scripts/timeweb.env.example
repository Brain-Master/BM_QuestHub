# Cloud API JWT (NOT S3 keys). Copy to scripts/timeweb.env (gitignored).
# Create/revoke: https://timeweb.cloud/my/api-keys
TIMEWEB_API_TOKEN=
TIMEWEB_APP_ID=
# Optional: override branch for deploy API (default: branch set on the app in Timeweb panel)
# TIMEWEB_DEPLOY_BRANCH=main
