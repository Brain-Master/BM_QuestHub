# WEB-SCHEDULE-06 — Close release verification and integrate supplied base mark

Repo /home/xipsin/projects/BM_QuestHub/design-previews/schedule-audit-2026-09-09/repo.
Branch fix/schedule-data-and-venues-2026-09-09; localbasea6498b5b599c58c219ab5ff97b5cdea59149e126.
Remote main expectedf5c1921889b0da7aa5e0a909db9a86562a0333ec (same tree).
Owner said Continue after explicit request for ONE marked synthetic lead to the
existing live spreadsheet/Telegram; attached Mechvarium brandboard now accessible
through /mnt/c Windows mount. Continue current release only, not unrelated CMS.

## Authority and exact boundaries

One synthetic UI lead through b-master.pro/existingreceiverd4ellekng389grh5rck4.
Dummy names and non-real phone+70000000000, clear TEST/DO NOT ENROLL/CALL comment.
Never click mos.ru enrollment, create a reservation, use real personal data,
or retry an uncertain/failed POST. Record durable private intent before sending;
read-only reconciliation by requestId and safephase logs. Existing configured
downstream Telegram/Sheets and optionaln8n delivery remain normal receiver behavior.
Block unrelated browserPOSTs including operationalpulse. Secret-use override from04
retained: exactlivefunction config credentials in memory only; Sheetsreadonly token,
columnC requestIDs and exactsyntheticrow readback; no unrelated applicant fields.
No webhook/getUpdates consumption, token changes, queue purge or trigger pause.

Source changes authorized for supplied logo: apps/web/public/sites/bm-base-moscow/
mechvarium-mark.svg; content/school-profiles.ts; content/venues/bm-base-moscow.yaml;
apps/web/lib/offers/annual-integration.test.ts; thisplan. Generatedoutputs protected
PA-GEN-001/PA-COMPAT-001..005: only compiler integrate-year-schedule.ts --write and
--check, exactexisting8snapshotkeys (stage only actual changed outputs). No manual
JSON edits. Preserve basepreparing/0groups/no photos and inactivegray behavior.
No original vector supplied: new SVG is an explicitly labelled web adaptation of
owner's compact mark, not a claim of original source/font extraction. Preserve
referenceimage unchanged; brandboard mockups are not realbase photographs.

Skill creating-brand-design-systems: evolution/product-surface/hybrid; scope only
base mark in existingUI, not a new siteidentity. Output editableSVG + proportional
private evidence/handoff package; existing site typography and statuscolors stay.

MOS runtime cutover remains HOLD pending audited backwardcompatible worker design.
No oldasyncdrain claim from timeout/emptyworkerqueue. ReadonlyauditsA/B before any
pipeline edits; exactadditional paths and acceptance must be recorded first.

## Validation/release

Logo/source tests: validSVG/localrefs, 16/24/32/48/64px, white/darkcontainers, grayscale,
base0groups/no photos; compiler/schema/tier tests; webcheck111routes; browserpreview.
Any pipeline change requires old/newmanifest contract tests and no archived writes.
Freeze exactbytes, independentreview, secret-scan, explicitstaging/stagedparity.
Owner's existing publication request continues: normalcommit/PR/main/Timeweb and
exactS3CAS publication afterfreshbackup. No unrelatedPR9/othercheckouts touched.
Lead existingversion30s revalidate live. One successfulmock or HTTP200 is not
durableleadproof: require UIoutcome +correlateddeliveryphase +Sheetreadback.
Failedpartial test remains recorded and not retried without explicitapproval.

Preflight: clean tree at localbase, indexempty before thisplan; Node22.23.1.
Memory registry had no relevanthits. Current liveGH/Timeweb readback pending.

## Verification evidence and release gate (2026-09-10)

Classification: product_domain (one existing venue identity); no reusable library.
Dependency direction unchanged: owner profile/YAML → compiler → map/manifest → site.
Auditors A Kant and B Goodall approved this exact six-feature-file scope before edits.
The two generated files are apps/web/data/v2/map-snapshot.json and
apps/web/data/v2/site-manifest.json; all other compiler outputs unchanged.
PA-GEN-001, PA-COMPAT-002/005/006/007 apply. Build artifacts under PA-BUILD-002/003
are validation-only. Existing PA-OPS-010 Timeweb release is permitted; no deployment
source files changed. Exact external writes are only data/v2/map-snapshot.json
and data/v2/site-manifest.json, with fresh backups, IfMatch and original ACLs.
No hot data, school photos, queues, triggers or cloud functions changed.

Lead test used one approved synthetic submission. Response502, requestId
3f42f79b-cbcc-4c7c-b80f-406623ea11b7; Telegram failed around10.6s with
upstream_network/unknown, Sheets not_started. Read-only requestId reconciliation
found zero matching Sheet rows. Never retry this test without new owner approval.
The wrapper hides the original cause and cannot distinguish fetch/body errors;
httpStatus0 is NOT a received HTTP status. IPv6/Undici diagnosis is unproven.
Independent receiver audits A/B recommend a separately approved closed read-only
runtime probe, no sendMessage/append/ops/n8n and no public debug endpoint.
Current30s receiver remains live; successful lead collection is NOT claimed.

MOS cutover remains HOLD: legacy manifests lack urlBatches/lifecycle, old results
need a fresh lifecycle/row-identity check before finalizer writes, progress needs
run-bound protection. Empty YMQ alone cannot prove old async executions drained.
No pipeline edit or function publication under06.

Passed: compiler --write then --check; annual-integration11tests; web check111
routes, four pre-existing lint warnings, no errors. Brand package validator22;
XML/safety and20renders (16/24/32/48/64px light/dark/gray/crop/blur); desktop1440
and mobile390 browser checks show correct SVG, preparing note and no overflow.
Private evidence /tmp/questhub-followup-c6OYva; source asset is the editable master.
Failure history: first compiler invocation from root failed TS alias resolution
before writes; corrected documented apps/web cwd. Initial local browser port was
closed; started task-owned static export server3195. First QA expected generic
inactive text, but the established preparing-specific copy is correct; corrected
assertion to that exact copy, without weakening group/status checks. Initial
snapshot backup lacked the remote merge object locally; no data was written.
Fetch exact main before the backup retry; no checkout/history changes.

Release order: first main/Timeweb contains SVG; confirm exact public asset bytes.
Then publish map followed by manifest only, with CAS and full readback. Rebuild
the same main SHA to refresh static S3 consumers; verify final DOM, not just SHA.
Helpers /tmp/questhub-followup-c6OYva/{github,snapshots,timeweb}.mjs are task-local,
not product files. Durable intents prohibit ambiguous PR/merge/PUT/rebuild repeats.
Rollback retains SVG while any map references it; restore only the two backed-up
objects after CAS against the published bytes, then rebuild approved prior state.
Stop on target drift, failed CI/review/scan, unknown intent, newer data or new scope.

Commit authorized: feat(web): apply supplied Mechvarium venue mark.
Staging is limited to the six feature paths above and this plan after final review.
Fresh secret-scan, exact frozen/staged parity, compilercheck and11 focused tests
are mandatory before commit. Current authority allows normal push/PR/main and
existing Timeweb/S3 publication, but does not authorize new infrastructure.
Feature fingerprint excludes this self-recording plan; all seven files receive
an independently reviewed full fingerprint recorded in the private freeze manifest.

Six-feature fingerprint (sorted relative path, NUL, bytes, NUL):
eb743dd3f5b11bfc9c3c51deb298594df9cc304bac47cafa1bc941973bc0a7d2.
Live main and successful Timeweb deployment still match f5c1921; exact main object
fetched locally, both cold S3 backups match that baseline. Candidate targets and
original bytes/ETags/ACLs saved privately. No external mutation yet under06.
