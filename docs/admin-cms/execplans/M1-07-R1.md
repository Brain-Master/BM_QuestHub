# ExecPlan M1-07-R1 — Close legacy token adapter review-evidence gaps

## 0. Metadata

- Status: `IN PROGRESS` (completed in final task report after push/CI)
- Classification: `repository_execution_record` / `test_evidence_correction`
- Repository: `Brain-Master/BM_QuestHub` (`D:/WORK/01_Active_Projects/BM_QuestHub`)
- Branch: `cms/m1-07-legacy-token-adapter`
- Base SHA: `6e508a108cb0e6222985134a3108708fd8d50e1d` (`origin/main`)
- Implementation HEAD (pre-corrective): `ae08f01af80b61ee48fd2733aee4f1312f22d337`
- Initial PR: `#3` OPEN / DRAFT / MERGEABLE
- Initial PR CI: `29872239104` / `success` / event `pull_request`
- Started at: `2026-07-22T02:51:00+03:00`
- Task Packet: owner prompt `BM QuestHub CMS — Corrective Task Prompt M1-07-R1`
- Agent: parent engineer-executor (Composer)
- Active repository policy: root `AGENTS.md`
- Active ExecPlan policy: root `PLANS.md`
- Canonical template: `docs/admin-cms/02_delivery/25_EXECPLAN_TEMPLATE.md`
- Commit policy: `test(admin): close legacy token evidence gaps`
- Push policy: push branch without force; keep draft PR #3; do not merge
- Corrective commit SHA is authoritative in Git/PR and the final task report;
  it is intentionally not self-embedded into the commit that creates this file.

## 1. Trigger

M1-07 implementation (`ae08f01…`) and initial Admin CMS CI (`29872239104`)
succeeded, but review readiness was blocked by incomplete test evidence and an
unfinished ExecPlan. Production adapter code was accepted as a correct candidate:

```ts
read() {
  return sessionStorage.getItem(LEGACY_TOKEN_STORAGE_KEY) ?? "";
}
write(value: string) {
  sessionStorage.setItem(LEGACY_TOKEN_STORAGE_KEY, value.trim());
}
```

M1-07-R1 closes evidence gaps only. No product behavior change. Not a roadmap
item; does not change the 174-task count. M1-08 remains unauthorized until
PR #3 is reviewed and merged.

## 2. Evidence gaps E1–E4

| ID | Gap | Severity |
|---|---|---|
| E1 | T5 proved only that get/set were not called during import while a storage stub was already installed; it did not prove import succeeds when the `sessionStorage` global is absent. False `const storage = sessionStorage` at module scope could pass. | review-blocker |
| E2 | Committed `docs/admin-cms/execplans/M1-07.md` said `Status: DONE` but still contained unfinished placeholders (`Final HEAD`, `Finished at`, `Final status`). | review-blocker |
| E3 | T3 asserted the expected key value but not that write touched exactly one storage entry. | evidence gap |
| E4 | T6 used `toThrow(boom)` without explicit `caught === boom` identity for read and write. | evidence gap |

## 3. Objective and non-goals

### Objective

Устранить E1–E4 без изменения production behavior: усилить T3/T5/T6 в существующих
восьми adapter tests, завершить `M1-07.md`, создать этот corrective ExecPlan,
опубликовать один corrective commit в draft PR #3 и получить новый успешный
Admin CMS CI.

### Non-goals

- any change to adapter / App / API / request-contract product behavior;
- changing test count or test names;
- M1-08 / M1-09 / M2;
- mark-ready / merge / force-push / tag / release / deploy;
- dependency, package/lock, config, or workflow changes;
- adding either ExecPlan to `MANIFEST.sha256`.

## 4. Authorized / forbidden paths

### Write (final — exactly three)

| Path | Role |
|---|---|
| `apps/admin/src/legacy-token-adapter.test.ts` | T3/T5/T6 evidence |
| `docs/admin-cms/execplans/M1-07.md` | complete placeholders; record E1–E4 supersession |
| `docs/admin-cms/execplans/M1-07-R1.md` | this corrective record |

### Transient (authorized only for N1–N3; must restore)

- `apps/admin/src/legacy-token-adapter.ts` — temporary mutations with exact
  SHA-256 restore to frozen product hash.

### Forbidden

Adapter/App/API/request-contract product files (except transient N probes),
other admin sources, package/lock/config, workflows, guard/secret-scan tooling,
AGENTS/PLANS/Makefile, MANIFEST/INDEX/PROTECTED/BASELINE/SECRET_SCAN/roadmap,
prior ExecPlans except `M1-07.md`, web/yandex/generated snapshots.

## 5. Live PR baseline

| Item | Required | Actual |
|---|---|---|
| Branch | `cms/m1-07-legacy-token-adapter` | match |
| HEAD | `ae08f01af80b61ee48fd2733aee4f1312f22d337` | match |
| Base `origin/main` | `6e508a108cb0e6222985134a3108708fd8d50e1d` | match |
| PR #3 | OPEN / DRAFT / MERGEABLE | match |
| PR commits/files | 1 / 6 | match |
| Initial CI | `29872239104` success on exact head | match |
| Reviews / threads | 0 / 0 | match |
| Index | empty | empty |
| Node / npm | `v22.23.1` / `10.9.8` | match (fnm multishell; system Node 24 not used) |

External dirty outside task (offers-snapshot, document package, scripts, plans)
left untouched and unstaged.

## 6. Frozen product hashes

Captured before edits; must equal at task end:

| Path | SHA-256 |
|---|---|
| `apps/admin/src/legacy-token-adapter.ts` | `9eb1a685644d502b867811d9769f22548dcc160d120d0261946142e826d644a4` |
| `apps/admin/src/App.tsx` | `d27ffd2529c7399278bc8a722cb84e740d357ecc10fb850c5436969dbfe1516e` |
| `apps/admin/src/api.ts` | `86f06a8ba527b56e3c51f79b51190c8ac6f8b7004c6c276a49a295c8901bb811` |
| `apps/admin/src/api.request-contract.test.ts` | `22f216b2497f259abfb04e82dfe0a29adbf8f5310db3eee3f816556aedb60a16` |

Baseline before edits: adapter 8/8, request 16/16, full 6/61, lint PASS,
typecheck PASS.

## 7. T3 / T5 / T6 corrections

Keep one describe, exactly eight tests, all names unchanged. Only T3/T5/T6
change materially; T1/T2/T4/T7/T8 remain semantically unchanged.

### T3 — `writes a trimmed token to the private legacy key`

After write:

```ts
expect([...store.entries()]).toEqual([
  [PRIVATE_KEY, "keep  internal"],
]);
```

Fails if an extra key is written.

### T5 — `accesses sessionStorage lazily after module evaluation`

Required sequence:

1. `vi.unstubAllGlobals();`
2. `vi.resetModules();`
3. assert `"sessionStorage" in globalThis` is false;
4. `await import("./legacy-token-adapter")` succeeds with global absent;
5. only then install synthetic storage;
6. call `read()` / `write()` and verify exactly one get and one set.

Fails on module-scope `const eagerStorage = sessionStorage`.

### T6 — `propagates storage failures without fallback`

Independent try/catch for read and write; `expect(caught).toBe(boom)` for each.
Does not rely only on `toThrow(boom)`.

## 8. N1–N3

Exact corrected test bytes saved before probes. Transient adapter mutations
restored to frozen SHA after each probe.

| Probe | Mutation | Required | Actual | Restore |
|---|---|---|---|---|
| N1 | module-scope `const eagerStorage = sessionStorage` | nonzero; T5 fails | exit 1; only T5 failed (`sessionStorage is not defined`) | exact SHA; 8/8 |
| N2 | `sessionStorage.setItem("syntheticExtraKey", "x")` in write | T3 fails | exit 1; T3 failed (extra entry); T5 also failed (setItem×2) | exact SHA; 8/8 |
| N3 | catch storage failure and throw new Error | T6 fails | exit 1; T6 failed (`caught !== boom`) | exact SHA; 8/8 |

No transient product mutation remains in the final diff.

## 9. M1-07 ExecPlan completion

`docs/admin-cms/execplans/M1-07.md` updated to:

- remove all placeholders;
- record implementation commit / initial PR #3 / CI `29872239104` / success;
- factual status `IMPLEMENTED / EVIDENCE CORRECTED BY M1-07-R1 / PENDING MERGE REVIEW`;
- explicit E1–E4 discovery after initial commit and supersession by M1-07-R1;
- final status wording that PR remains OPEN/DRAFT and M1-08 is not authorized;
- no claim that the original T5 already proved absent-global import.

## 10. Validation

```text
Baseline (pre-edit @ ae08f01):
  adapter 8/8
  request 16/16
  full 6/61
  lint/typecheck PASS

Post-correction (local, pre-commit):
  adapter 8/8
  request 16/16
  full 6/61
  0 fail/skip/todo
  lint/typecheck/build/npm ls PASS
  dist removed
  guard tests + preflight --base 6e508a1 PASS
  secret-scan PASS
```

Final command outcomes are authoritative in the task report after freeze.

## 11. Source invariants

Final hashes must equal pre-task hashes for adapter / App / API /
request-contract and all other product/config/package/lock/workflow/tooling
files. Production behavior remains exactly the M1-07 candidate.

## 12. PR scope

Expected after corrective commit + push:

```text
PR #3 remains OPEN / DRAFT
2 commits / 7 files
seventh file: docs/admin-cms/execplans/M1-07-R1.md
new Admin CMS CI on exact corrective head / pull_request / success
```

## 13. Acceptance

R1–R57 from the Task Packet; no PARTIAL. Key proofs: T3 exact one entry,
T5 absent-global import then later read/write, T6 read/write error identity,
N1–N3 fail-and-restore, product hashes unchanged, three authorized writes only,
exact commit message, push without force, PR stays draft, M1-08 not started.

## 14. Freeze / review

Freeze paths:

```text
apps/admin/src/legacy-token-adapter.test.ts
docs/admin-cms/execplans/M1-07.md
docs/admin-cms/execplans/M1-07-R1.md
```

Algorithm: SHA-256 over sorted records `(relpath NUL bytes NUL)`.
Per-file digests and combined fingerprint are recorded in the final task report
after freeze (this file’s own bytes change when digests are embedded; corrective
commit SHA is not self-embedded here).

Reviewer verifies false-pass resistance, completed records, and zero product
drift. Required verdict: `APPROVE`.

If subagents unavailable:

```text
Independent subagents unavailable; parent performed a separate read-only
test-evidence and execution-record review.
```

## 15. Git / CI actions

1. Stage exactly the three authorized paths (never `git add .` / `-A`).
2. `git diff --cached --name-status` and `--check`.
3. Commit: `test(admin): close legacy token evidence gaps`.
4. `git push origin cms/m1-07-legacy-token-adapter` (no force).
5. Wait for Admin CMS CI on exact corrective head, event `pull_request`,
   conclusion `success`.
6. Update PR #3 body with M1-07-R1 summary; do not change title/base/draft.
7. Do not merge.

## 16. Final status

Owned by the final task report after push and CI:

- corrective commit SHA (Git/PR authoritative; not self-embedded here);
- new CI run ID / success;
- PR #3 = 2 commits / 7 files / OPEN / DRAFT;
- product invariants unchanged;
- M1-08 / M1-09 / M2 not started;
- merge readiness remains with `M1-07-REVIEW`.
