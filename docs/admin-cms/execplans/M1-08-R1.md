# ExecPlan M1-08-R1 — Complete auth-failure execution evidence

## 0. Metadata

- Status: `DONE / APPROVE / PENDING M1-08-REVIEW`
- Classification: `repository_execution_record` (corrective evidence only)
- Repository: `Brain-Master/BM_QuestHub` (`D:/WORK/01_Active_Projects/BM_QuestHub`)
- Branch: `cms/m1-08-auth-failure-state`
- Base SHA: `3e772371adb9f72c7843e315378f7eae8bc3d528` (`origin/main`)
- Pre-corrective HEAD: `78c659afc4efa01aceac9116385ba8f16a1a53e2`
- Initial PR: `#4` OPEN / DRAFT / MERGEABLE
- Initial PR scope: `1 commit / 6 files`
- Initial Admin CMS CI: `29883544320` / `pull_request` / `success` / head `78c659af…`
- Task Packet: owner prompt `BM QuestHub CMS — Corrective Task Prompt M1-08-R1`
- Agent: parent execution-evidence / integration engineer (Composer)
- Active repository policy: root `AGENTS.md`
- Commit policy: `docs: complete M1-08 execution evidence`
- Push policy: push without force; keep same draft PR; do not merge

```text
Corrective commit SHA is authoritative in Git/PR and the final task report;
it is intentionally not self-embedded into the commit that creates this file.
```

## 1. Trigger

Committed `docs/admin-cms/execplans/M1-08.md` still claimed:

```text
Status:
IMPLEMENTED / PENDING DRAFT PR CI

Implementation commit:
recorded after commit (authoritative in final report)

PR:
recorded after draft creation
```

and Final status said implementation was “complete locally” with commit/PR/CI
“filled later” in the chat report. That is no longer factual after commit
`78c659af…`, draft PR `#4`, and CI `29883544320` success. PR body also left
the CI test-plan checkbox unchecked.

## 2. Objective

Close the execution-evidence gap without changing production or test behavior:

1. complete factual fields in `M1-08.md`;
2. record this corrective ExecPlan;
3. one corrective commit on the existing branch;
4. obtain `Admin CMS CI` success on the corrective head;
5. update PR `#4` body after that success.

## 3. Non-goals

- any change to auth-failure-state / adapter / App / tests;
- M1-09 / M1-10 / M2 / M3;
- mark ready / merge / force push / tag / release / deploy;
- roadmap counter increment;
- MANIFEST updates.

## 4. Live PR baseline

| Item | Value |
|---|---|
| PR | https://github.com/Brain-Master/BM_QuestHub/pull/4 |
| Title | `M1-08: add auth-failure state contract` |
| State | OPEN / DRAFT / MERGEABLE |
| Base ← head | `main` ← `cms/m1-08-auth-failure-state` |
| Head | `78c659afc4efa01aceac9116385ba8f16a1a53e2` |
| Scope before R1 | 1 commit / 6 files |
| Reviews / review comments | 0 / 0 |
| Initial CI | `29883544320` success |
| Node / npm | v22.23.1 / 10.9.8 |
| External dirty fingerprint | `41563d81ded1fdf2068b04095c5e2ff7afa36b25e50daba6ca280c1b9407a82c` |

## 5. Confirmed stale-evidence gap

| Field | Stale value | Factual value |
|---|---|---|
| Status | `PENDING DRAFT PR CI` | Initial CI already success |
| Implementation commit | “recorded after…” | `78c659af…` |
| PR | “recorded after…” | `#4` OPEN/DRAFT |
| Final status | local-only / fill later | commit + CI completed |

Review readiness was blocked solely by this stale evidence; implementation
candidate behavior remains accepted.

## 6. Authorized / forbidden paths

### Authorized (exactly two)

- `docs/admin-cms/execplans/M1-08.md`
- `docs/admin-cms/execplans/M1-08-R1.md`

### Forbidden

All product/test/tooling/workflow/package paths and prior ExecPlans other than
`M1-08.md`. No MANIFEST entry for either ExecPlan.

## 7. Frozen implementation hashes / fingerprint

Algorithm: SHA-256 over sorted `path NUL bytes NUL` (working-tree bytes), same
as `M1-08.md` §23.

| Path | SHA-256 (WT) |
|---|---|
| `apps/admin/src/App.tsx` | `a3c4bf5602f2c779dc4956ef1b864d295bd5f4b65275723511630a5f23ec6746` |
| `apps/admin/src/auth-failure-state.test.ts` | `1b7de59cbc8eacc736bb6bd4e879414596ae6432fca4a7fb4ccf41187502cb80` |
| `apps/admin/src/auth-failure-state.ts` | `3ea124a24090ec182a8e5325a6d054b4a2259f60507b1e880da0e17521a97c69` |
| `apps/admin/src/legacy-token-adapter.test.ts` | `00e15f879014680ea0747eabebb2da439da1e32dce6cdf70dbf6c0d9f0cc1190` |
| `apps/admin/src/legacy-token-adapter.ts` | `2fbe578bb41855932821bb38dae0dfef80c0aa4e11875e3f31f823f6283033b2` |

Reported six-path implementation freeze fingerprint (re-verified before R1
edits, including committed `M1-08.md` text under `78c659af…`):

```text
52e7665f2661fb9e53023c33e4db8c54b6dc36cdc42a3dcb9b4e61a0dc699c4c
```

`api.ts`, request-contract test, package/lock/config, guard and secret-scan
tooling: blob equality to HEAD — unchanged.

## 8. Baseline validation

Before documentation edits:

| Gate | Result |
|---|---|
| auth-failure-state | 10/10 |
| legacy-token-adapter | 10/10 |
| api.request-contract | 16/16 |
| full admin Vitest | 7 files / 73 tests |
| lint / typecheck / npm ls / build | PASS |
| dist removed | PASS |
| guard tests | 16/16 |
| preflight (LF-manifest wrap) | PASS `changed=6` |
| secret-scan | PASS |

## 9. M1-08 metadata correction

Replaced stale status with:

```text
IMPLEMENTED / INITIAL PR CI PASS / EVIDENCE COMPLETED BY M1-08-R1 /
PENDING MERGE REVIEW
```

Recorded exact implementation commit, finished timestamp
`2026-07-22T04:35:32+03:00`, PR `#4` identity/scope, CI run/event/head,
corrective task reference, and merge-readiness ownership.

## 10. M1-08 final-status correction

Replaced local-only final status with factual commit + initial CI success,
frozen product behavior, R1 ownership of evidence completion, PR remaining
OPEN/DRAFT, and M1-09/M1-10 unauthorized. Historical corrective note added in
validation log; timeline not rewritten as if R1 preceded `78c659af…`.

## 11. PR body correction

After corrective CI success, update PR `#4` body to:

- preserve implementation summary;
- add M1-08-R1 evidence-completion note;
- record old stale status and correction;
- state product/test unchanged + fingerprint match;
- record focused/full gates and new CI run;
- scope `2 commits / 7 files`;
- mark CI checkbox `[x]`;
- keep OPEN/DRAFT; no merge claim.

## 12. Source invariants

No edits to auth-failure-state, adapter, App, tests, API, package/lock/config,
workflows, guard, or secret-scan. External dirty fingerprint unchanged at
`41563d81…a82c`.

## 13. Validation

After documentation changes, re-run full admin test/lint/typecheck/ls/build,
guard tests, preflight, and secret-scan. Require identical accepted results
and exactly two changed ExecPlan paths.

## 14. Acceptance

R1–R51 matrix in final report; no `PARTIAL`.

## 15. Freeze

Frozen after documentation edits (WT bytes):

| Path | Role |
|---|---|
| `docs/admin-cms/execplans/M1-08.md` | corrected execution record |
| `docs/admin-cms/execplans/M1-08-R1.md` | this corrective record |

Per-file SHA-256 and combined fingerprint recorded in final task report after
freeze (and again after commit for Git identity).

## 16. Review

```text
Independent subagents unavailable; parent performed a separate read-only
execution-evidence and source-invariant review.
```

Checks: factual lifecycle; no stale CI status; no placeholders; implementation
hashes unchanged; no product/test drift; no premature merge claim; M1-09/M1-10
unauthorized.

Verdict: `APPROVE`

## 17. Git / CI actions

- Stage exactly two ExecPlan paths
- Commit: `docs: complete M1-08 execution evidence`
- Push without force to `cms/m1-08-auth-failure-state`
- Wait for new `Admin CMS CI` `pull_request` success on corrective head
- Update PR body after success
- Do not create a second PR; do not mark ready; do not merge

## 18. Final status

M1-08 execution evidence is factually complete. Product/test behavior is
unchanged. PR `#4` remains OPEN/DRAFT pending `M1-08-REVIEW`. M1-09 and
M1-10 are not authorized. Roadmap acceptance remains `19 / 174` until merge.
