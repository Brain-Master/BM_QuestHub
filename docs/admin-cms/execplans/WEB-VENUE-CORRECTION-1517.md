# WEB-VENUE-CORRECTION-1517 — first step of the venue corrections

## 0. Metadata / owner Task Packet
Status: DONE (local first slice only). Started 2026-09-23, Europe/Moscow.
Repository Brain-Master/BM_QuestHub; branch fix/schedule-data-and-venues-2026-09-09;
expected HEAD b789b70d00747cd1078cb72dd295652fad53ac83. NO COMMIT.
Owner asks to correct the venue audit findings together, step by step, starting
with the explanation of school1517 availability. This first slice is local only.

## 1. Objective
Reconcile the five1517 cards with current public MOS facts, preserve all other
groups, and stop presenting all scheduled groups as open enrollment.

## 2. Non-goals
No production/cloud/S3 writes, deployment, staging, commit, push, new dependencies,
teacher/age guesses, school937 promotion, other-school refresh, or score rewrites.
The previous audit remains a historical observation, with a separate correction.

## 3. Authorized paths / protected operations
Source edits: scripts/refresh-annual-selection.mjs and its test; component
apps/web/components/site-selection-grid.tsx; new lib/sites/site-stats.test.ts;
scripts/verify-venue-availability-ui.mjs; docs/venue-corrections-2026-09-23/; this plan.
Test maintenance within this slice: apps/web/lib/offers/annual-integration.test.ts,
course-finder.test.ts (compare admission with the registry, not a frozen count);
apps/web/lib/offers/school-2103.test.ts (historical51 hash reads the same frozen
fixture, without changing its hash or current-data assertions);
scripts/school-37-import.test.mjs and dated immutable fixtures
scripts/fixtures/annual-69-before-1517/{registry,offers}.json. Historical hash guards
remain exact; migration tests use frozen public inputs instead of mutable current
seat data. New selection tests verify preservation against the actual preimage.
Generator-only PA-GEN-001/PA-COMPAT-003 writes: apps/web/content/annual-mos-refresh.generated.json
and apps/web/data/offers-snapshot.json. PA-COMPAT-005 permits generator-only
apps/web/data/v2/site-manifest.json if required by hot-tier compiler (confirm tier).
Hot tier verified to exclude ALL cold artifacts (including manifest; no write needed).
All other generated source and cold artifacts must remain unchanged; run existing
integrate-year-schedule.ts with --tier=hot only. Back up exact registry/offers bytes
before replacement. Existing source revision remains unchanged; no CSV migration.
The selection generator alone may create/remove its exclusive UUID-named
apps/web/content/.annual-selection-<UUID>.tmp for same-filesystem atomic rename;
exact created path is operation-owned, never staged or retained on normal completion.
PA-BUILD-002/003: apps/web/out and .next generated by web check only; never staged.
Build also regenerates apps/web/public/host-aliases.json using its existing
generator; require byte-identical output. verify-course-finder-ui.mjs may correct
its stale five-closed-groups report wording without weakening assertions.
Read-only: public numeric MOS GET and public published offers; scoped workflows
for explanation. No credentials. Forbidden: secrets, workflows, services/**,
WEB-PUBLISHING-2044 plan and all unrelated concurrent work. No policy edits.

## 4. Preflight
2026-09-23 MSK, PRE-FREEZE: pwd/rev-parse/branch/HEAD/status/log/diff-check exit0;
Node22.23.1/npm10.9.8 via pinned PATH. Prior69-group/19-venue dirty candidate retained.
services/publications plus WEB-PUBLISHING-2044 belong to another active scope and
are excluded. Compiler --check exit0 (69groups/70slots/19venues). Standalone refresh
test exit0 (1test). Index empty. No baseline implementation failure.

## 5. Architecture and classification
product_domain: bounded local selection refresher uses existing public MOS adapter
and refreshAnnualCards identity/location/partial-field/lifecycle guards. Select by
explicit source group IDs; fail on unknown/unregistered selection before network.
Global registry coverage remains partial (never claim69 verified after5 requests).
Only selected records and their successful card errors may change; unrelated
records/candidates/errors/source bytes remain intact. Compare preimages before
writing to detect concurrent edits; backup registry. Existing hot compiler owns
public offers, preserving unrelated offers and editorial teacher/price rules.
UI: wording only; scheduled-group count and inactive-map semantics unchanged.

## 6. Read-only audits
A architecture/data-flow and B safety/tests: pending independent agents; parent
verifies accepted findings. Only parent writes.
AuditB Dewey confirmed all scheduled groups count includes closed/sold-out; preserve
semantics and both views. Existing refresh loses unrelated card errors if reused
wholesale: wrapper merges selected groups only, keeps unrelated MOS_TIMEOUT.
Require exactly5 verified, not success via archived coverage. Accepted test-boundary
and host-alias generation findings; added exact paths above before relevant writes.
AuditA Helmholtz confirmed five exact identities, hot-only writes, historical
fixtures, and a missing effective-studyYear guard. Accepted: validate reviewed
group-code mapping and effective studyYear before any replacement; require
unchanged64 other annual rows,56 other registry records and all cold bytes.

## 7. UX contract
Parent can distinguish a scheduled group from admission and available seats.
Directory cards/list say groups 'в расписании', not 'открыто'. Existing individual
status/capacity behavior stays intact. No new controls, focus/route/layout changes.
Reference: previous venue audit findings and existing schedule-board admission
contract; loaded static Next export guide. No visual identity redesign.

## 8. Security/privacy
Only anonymous public GETs to exact MOS card IDs, existing response/time bounds.
No enrollment, notifications, private chats, applicant data or secrets. Tests fake
HTTP at the existing fetch boundary; browser blocks external/unsafe requests.

## 9. Steps / negative paths
1. Audit selection and UI behavior; document current source versus published data.
2. Add local fail-closed selection generator and negative tests; fix misleading copy.
3. Refresh exactly five1517 identities, hot compile, verify all other records unchanged.
4. Full web validation, local browser checks, evidence report, freeze/fresh review.
Negative: unknown IDs, duplicate/empty selection, source mismatch, archived groups,
identity/location changes, timeout/partial fields, incomplete pass, stale/current
output conflict, no destructive overwrite, false global success, unrelated data loss.

## 10. Test plan / acceptance
Existing baseline commands: node --test scripts/annual-standalone-refresh.test.mjs;
node apps/web/node_modules/tsx/dist/cli.mjs --tsconfig apps/web/tsconfig.json
scripts/integrate-year-schedule.ts --check; npm --prefix apps/web run check;
git diff --check. New authorized selection tests and local browser script above.
Offers/sites tests run from apps/web using node --import tsx --test.
A1 exact5 refreshed with source/link/date evidence; A2 all other data preserved;
A3 grid/list copy truthful and counts unchanged; A4 tests/build/browser pass;
A5 freeze and independent APPROVE. All pending.

## 11. Validation and failure history
PRE-FREEZE read-only evidence at2026-09-22T21:34Z: all5 current MOS cards open,
free1/4/1/2/2,total15; saved registry from09Sep had total12, four closed.
Public S3 GET200 generated21Sep but allfive refreshedAt still09Sep: snapshot build
date is NOT source freshness. Second diagnostic pass had one MOS_TIMEOUT for
923308 and4successes; no writes. Preserve failure, no claim recurring sync fixed.
Exploratory rg named two nonexistent candidate module paths, exit2; actual paths
located from source. Web reader inaccessible for b-master page; directGET200.

## 12. Scope changes / stop conditions
None. Stop for incompatible source revision, unexplained overlapping changes,
loss of identity, mandatory gate failure or need for production/credential access.
Owner answered during this turn: Lokteeva Irina Dmitrievna,6–13. Recorded in the
correction report; activation of937 belongs to the next explicitly bounded step.

## 13. Freeze/review
2026-09-22T21:57:50.058Z: frozen83files, SHA256
9d8bf5e6610f1792e37067e5eeb186ea201683473ad08ef464ddcaa73b07ad05.
HEAD b789b70d00747cd1078cb72dd295652fad53ac83. Hash sorted relative path,NUL,bytes,NUL.
Included: union of git diff --name-only -z and git ls-files --others
--exclude-standard -z, excluding services/** and docs/admin-cms/execplans/**.
Thus prior37/profile/audit product files remain covered for integrity, not claimed
as new work in this slice. Exclusions protect unrelated publications work and
self-referential execution records. Index empty; diff-check PASS. No edits after
freeze. Fresh independent review pending; no publication authority.

## 14. Staging/final status
Staging NOT AUTHORIZED. No commit/push/deployment. First slice not yet complete;
remaining venue corrections not automatically declared complete.
Do not push.
Do not start the next task.

## 15. Final validation evidence (2026-09-23 MSK, PRE-FREEZE)
All commands use Node22 PATH at /home/xipsin/.nvm/versions/node/v22.23.1/bin.
Root unless stated. Live selective run at00:52MSK exit0: exact5 success,
global incomplete69 retained. Registry backup /tmp/questhub-annual-selection-rHoNA0;
hot compiler backup /tmp/questhub-year-data-backup-77ygHm. No source/cold mutation.

- node scripts/refresh-annual-selection.mjs --group-id=listing:2415012
  --group-id=К7648-26 --group-id=К7651-26 --group-id=listing:2542312
  --group-id=К7733-26 --write --report=<absolute docs/venue-corrections-2026-09-23/school-1517-refresh.json>
  exit0; exact report contains all before/after values and timestamps.
- node apps/web/node_modules/tsx/dist/cli.mjs --tsconfig apps/web/tsconfig.json
  scripts/integrate-year-schedule.ts --write --tier=hot, then --check --tier=hot:
  both exit0;69groups/70slots/19venues. Only hot artifact written.
- apps/web cwd: node --import tsx --test lib/offers/*.test.ts lib/sites/*.test.ts
  exit0,91/91. Historical51/60 hashes unchanged; fixtures byte hashes checked.
- root: node --test scripts/refresh-annual-selection.test.mjs
  scripts/annual-standalone-refresh.test.mjs scripts/school-37-import.test.mjs
  scripts/school-2103-import.test.mjs scripts/school-937-intake.test.mjs
  scripts/annual-overlay-refresh.test.mjs scripts/lib/mos-annual-cards.test.mjs
  exit0,37/37. After final midnight-case addition selective suite8/8 exit0;
  complete pipeline re-run with --test-reporter=dot exit0,38/38.
  Atomic same-directory write improvement then revalidated by selective8/8;
  no further real network refresh/write needed, data bytes unchanged.
- SITE_SNAPSHOT_SOURCE=local OFFERS_SNAPSHOT_SOURCE=local
  NEXT_PUBLIC_OFFERS_SNAPSHOT_URL=/data/offers-snapshot.json
  NEXT_PUBLIC_SITE_URL=https://b-master.pro npm --prefix apps/web run check
  exit0:120pages, schemas/lint/type/build gates pass;4existing lintwarnings,
  existing NFT warning. S3 probe SKIPPED(localmode), not a production PASS.
- QA_OUTPUT=<absolute docs/venue-corrections-2026-09-23/qa>
  node scripts/verify-venue-availability-ui.mjs exit0, ninebrowserjourneys,
  exactlinks/seats/checkdates,0overflow/JSerrors/writes;3axe scans0violations,
  no incomplete nodes within finder scope.64otherannual/56otherregistry preserved.
  Mobile screenshot inspected; full-page capture is not physical device testing.
- make secret-scan exit0,1399files; git diff --check exit0; index empty.

Protected unchanged hashes after refresh/build:
source101f89158a7310f4adc798a287b6f2711fa073c042da77490cce8c66e170bc68;
catalog6b5d626f8f4d7d460b7e630b6a34933f62ad37f8e1a45c511062bcec680ada7a;
map e74228cb176e87218e0f35470dd78710406f6c9c01d352abc276a674562393b3;
manifest565a9da33368f259dca2426cf72e1debc4656e83510ceb051b29e9cbcc0eeabf;
all4annualdetails and host-aliases hashes match preflight (recorded tool evidence).
Updated registry2e38a2a0bbc8431cc7364db62d9b8145b621f6e6df2a4397b87ca634e4a50c65;
offers70b3d81d8ceef401a8a10d5de7576f28a9a8cc17c45b9e9ac206b11ace1fb222.

Additional diagnostic failures: fixture-patch emitter first used bare node without
pinnedPATH and failed before writes; explicit executable corrected. Two new fixture
files initially landed at wrapper-relative paths; apply_patch moved these exact
agent-created files to the intended repository, exact hashes verified. One root
test path was invoked from apps/web (no matching file); rerun at root matched8tests.
These failed invocations are not counted as passing tests. No mandatory actual
implementation test has failed; original source, migration guards and prior user
changes preserved.

Frozen verification: complete38pipeline exit0 after atomic writer change;
secret-scan1399PASS. Additional node scripts/verify-school-37-ui.mjs exit0:
ninegroups, exactdirectlinks, history/reload/QR/mobile/focus, privacy1202files,
0writes/JSerrors/overflow. Three axe scans0violations;320px has one incomplete
color-contrast finding, so not claimed as complete accessibility verification.
Artifacts written only to /tmp/questhub-school37-qa, not frozen report paths.
Recomputed83file fingerprint matches9d8bf5e...; staged index empty.

## 16. Independent final review / completion
Kant (01a0cb20-ca8b-7151-98af-2e81b26538aa): APPROVE, no blocking findings.
Reviewer independently matched83file fingerprint, ran34targeted tests plus
year/clock/historical guards, confirmed64/56record preservation, unchanged errors
and candidates, checked atomic preimages and explicit local-only documentation.
No edits to frozen bytes after review. A1–A5 PASS for1517 local slice only.
No staging/commit/push/deployment; branch and HEAD unchanged. Source/cold hashes
unchanged as above. Prior dirty37/profile/audit work retained, concurrent services
excluded and untouched. Production and regular-sync repair NOT DONE; othervenue
findings remain.937 ownerfacts now confirmed but its integration not started.
Next task started: NO. Final handoff includes local preview and evidence report.
