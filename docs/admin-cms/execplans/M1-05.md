# ExecPlan M1-05 — Create snapshot fixtures

## 0. Metadata

- Status: `DONE`
- Classification: see § Component classification
- Repository: `Brain-Master/BM_QuestHub` (`D:/WORK/01_Active_Projects/BM_QuestHub`)
- Branch: `main`
- Base SHA: `aa45e868491435697d794214634cb02639c9665d`
- Final HEAD: `aa45e868491435697d794214634cb02639c9665d` (unchanged)
- Started at: `2026-07-21T07:26:00+03:00`
- Finished at: `2026-07-21T07:33:00+03:00`
- Task Packet: owner prompt `BM QuestHub CMS — Task Prompt M1-05`
- Agent: parent engineer-executor (Composer)
- Active repository policy: root `AGENTS.md`
- Active ExecPlan policy: root `PLANS.md`
- Canonical template: `docs/admin-cms/02_delivery/25_EXECPLAN_TEMPLATE.md`
- Commit policy: `NO COMMIT`
- Push policy: `DO NOT PUSH`

## 1. Objective

Создать минимальные reusable synthetic valid/invalid fixtures для текущего
flat snapshot bundle, подключить их к существующим M1-04 тестам и доказать
детерминизм, fresh object graphs и отсутствие production/PII/secret-like data
без изменения production source.

## 2. User value

Тестовые входы snapshot envelope становятся единым synthetic factory API:
повторные inline envelopes не размножаются; integrity suite блокирует
дрейф vocabulary и shared mutable graphs до появления selectors (M1-06).

## 3. Non-goals

- production snapshot data / golden public contracts (M9-01);
- deep version compatibility (M1-09);
- read model selectors (M1-06);
- runtime parser / `api.ts` changes;
- App / UI / backend / web / generated snapshot edits;
- fixture generators, update CLI, serializers;
- new dependencies;
- stage / commit / push;
- start M1-06 / M1-09.

## 4. Authorized / forbidden paths

### Write (final)

| Path | Classification |
|---|---|
| `apps/admin/src/test-fixtures/snapshot-bundle.fixtures.ts` | `repository_test_fixture` |
| `apps/admin/src/snapshot-fixtures.test.ts` | `repository_test` |
| `apps/admin/src/snapshot-boundary.test.ts` | `repository_test` |
| `apps/admin/src/api.request-contract.test.ts` | `repository_test` |
| `docs/admin-cms/execplans/M1-05.md` | `repository_execution_record` |

### Transient authorized

- `apps/admin/src/test-fixtures/snapshot-bundle.fixtures.ts` — controlled
  regression probes with exact byte restoration;
- `apps/admin/dist/**` (`PA-BUILD-001`) — generate then remove.

### Forbidden

All other paths per Task Packet §6, including `api.ts`, `snapshot-boundary.ts`,
App, backend, package/lock, config/smoke, governance, workflows, generated
snapshots, M1-04 / M1-04-R0 ExecPlans, M1-06+.

## 5. Preflight

```text
root: D:/WORK/01_Active_Projects/BM_QuestHub
remote: origin https://github.com/Brain-Master/BM_QuestHub.git
branch/HEAD: main / aa45e868491435697d794214634cb02639c9665d
fnm: 1.39.0 (WinGet Schniz.fnm; activated via absolute path — bare `fnm` not on PATH)
node: v22.23.1 (fnm_multishells …\node.exe) — NOT system Node 24
npm: 10.9.8 (same multishell)
make: GNU Make 4.4.1
git index: empty
git diff --check: clean (LF/CRLF warnings only on unrelated dirty paths)
```

Path checks:

| Path | Expected | Actual |
|---|---|---|
| `apps/admin/src/api.ts` | present | present |
| `apps/admin/src/snapshot-boundary.ts` | present | present |
| `apps/admin/src/snapshot-boundary.test.ts` | present | present |
| `apps/admin/src/api.request-contract.test.ts` | present | present |
| `apps/admin/src/test-fixtures/` | absent | absent (pre) |
| fixture module / fixture test | absent | absent (pre) |
| `docs/admin-cms/execplans/M1-04.md` | present | present |
| `docs/admin-cms/execplans/M1-05.md` | absent | absent (pre) |
| `apps/admin/dist` | absent | absent |

Worktree: pre-existing external dirty from M0/M1 series (unchanged by this task
except authorized M1-05 paths).

## 6. M1-04 prerequisite / full hashes / fingerprint

| Check | Expected | Actual | Result |
|---|---|---|---|
| Status | DONE / APPROVE | DONE / APPROVE (`M1-04.md`) | PASS |
| Primary fingerprint | `26dc2a053f92173f416df51e528608a007d38b825f4e84aa228fc1209076d6d3` | recomputed match | PASS |
| `api.ts` | `8dc0e8216e8b9062068bddccc2f13e6c97bd943bf8d1a2a15d4c3d69e142edc3` | match | PASS |
| request-contract | `9c15498a3569e1f3139fe01008a83478e1fa7d34cda3189e512b6b5fe065f9b1` | match (pre) | PASS |
| `snapshot-boundary.ts` | `8a092ad58e765115fe0239aee3a6a443276d2e44c8f934ff977064851b2a8b27` | match | PASS |
| boundary test | `919b1efa977cc8b610df003d5c621681d98a51818316ae54b8bd70a72b97e4ab` | match (pre) | PASS |
| Prefix/suffix vs Task Packet | 26dc2a05…76d6d3 / 8dc0e821…42edc3 / 9c15498a…65f9b1 / 8a092ad5…2a8b27 / 919b1efa…97e4ab | compatible | PASS |
| A1–A109 | PASS | recorded PASS | PASS |
| M1-05 started | no | ExecPlan absent pre | PASS |

### M1-04-R0

| Check | Expected | Actual | Result |
|---|---|---|---|
| Status | DONE / APPROVE | DONE / APPROVE | PASS |
| Full-file SHA-256 | `4961b25142ba3824a0f044a470545c508fa1d91f4ab0a65f3271e621331ec579` | match | PASS |
| Path fingerprint | `c3c60eee76fac91387d6b302c264f36b8bef9f6cf85f047a20a8d71fa253d68f` | accepted | PASS |

## 7. Protected-data rule

Fixtures are synthesized only from the accepted M1-04 type/parser field names
(`ok`, `catalog`, `map`, `site`, `manifest`, `offers`) and the Task Packet
closed string vocabulary. No reads of `apps/web/data/**`, `snapshots/**`, S3,
Sheets/Drive, live admin API, logs, or deployment captures.

## 8. Auditor A — fixture extraction

| Evidence | Finding |
|---|---|
| `snapshot-boundary.test.ts:10-20` (pre) | valid object-root inline envelope |
| `snapshot-boundary.test.ts:23-34` (pre) | valid nullable inline envelope |
| `snapshot-boundary.test.ts:205-219` (pre) | additive field inline envelope |
| `api.request-contract.test.ts:7-15` (pre) | success mock duplicates nullable envelope |
| `api.request-contract.test.ts:381-393` (pre) | malformed success duplicates envelope + `catalog: []` |
| `snapshot-boundary.ts:13-21` | `SnapshotsBundle` flat shallow contract |
| `snapshot-boundary.ts:39-46`, `13-37` | error/issue contracts |
| glob `test-fixtures/**` | no shared fixture module pre-task |
| test imports | no `apps/web/data` / production snapshot imports |

**Conclusion A:** The repeated synthetic flat bundle can be extracted into
test-only fresh factories without changing production behavior or test
semantics.

## 9. Auditor B — fixture safety / false confidence

| Risk | Mitigation |
|---|---|
| Copied generated JSON | not opened; synthetic literals only |
| Production titles/slugs/URLs | closed vocabulary enforced by F5 |
| Emails/phones/addresses | F5 + R3 probe |
| Secret-like tokens | F5 regex deny-list |
| Timestamp/random/UUID | no Date/Math.random/crypto; F5 patterns |
| Singleton mutable exports | factories only; no module-level mutable exports |
| Shared nested constants | each factory builds new nested objects |
| `undefined` instead of missing | I3/I5 omit properties; verified `hasOwnProperty` false |
| Invalid fixture accepted | F3 + R2 |
| Valid fixture relies on deep schema | shallow markers only (`fixtureKind`) |
| Fixture imports `api.ts` | type-only import from `snapshot-boundary` |
| Fixture imports test framework | none |
| Weakened existing assertions | 13+16 names retained; privacy marker kept on malformed derive |
| False-pass fixture count | exactly 5 named tests |
| Unused fixture file | imported by integrity + boundary + request tests |
| Package/config/production source change | forbidden; hashes unchanged |
| Invented M1-06 fields | none |

**Conclusion B:** Proceed with closed-vocabulary factories and integrity suite.

## 10. Fixture design

- Synthetic, minimal, deterministic, JSON-compatible, human-reviewable.
- Fresh root + nested objects per call.
- Not a runtime contract or second source of truth.
- Bounded: module ≤ 240 lines / 14 KiB; serialized fixture data ≤ 4 KiB.

## 11. Closed vocabulary

Exact Task Packet allowlist for string values (fixture data + expected issue
fields + invalid IDs). Property names allowed separately. No prose, domains,
URLs, PII, credentials.

## 12. Valid factories

1. `createValidSnapshotBundleFixture()` — nullable roots + additive
   `fixtureMetadata`.
2. `createValidSnapshotBundleWithObjectOptionalsFixture()` — object-valued
   `manifest` / `offers`.

No singleton valid exports.

## 13. Invalid fixture table

| ID | First issue |
|---|---|
| `root-null` | `$` / INVALID_TYPE / object |
| `ok-false` | `$.ok` / INVALID_VALUE / literal true |
| `missing-catalog` | `$.catalog` / REQUIRED / object (property absent) |
| `catalog-array` | `$.catalog` / INVALID_TYPE / object |
| `missing-manifest` | `$.manifest` / REQUIRED / object\|null (property absent) |
| `offers-string` | `$.offers` / INVALID_TYPE / object\|null |

Factory: `createInvalidSnapshotBundleFixtures()` → fresh array/graphs each call.

## 14. Freshness / determinism contract

F4: deep-equal across twin calls; distinct root/nested identities; local
mutation isolation; no shared global state.

## 15. Existing-test reuse

- Boundary B1 object roots → `createValidSnapshotBundleWithObjectOptionalsFixture()`
- Boundary B2 nullable → `createValidSnapshotBundleFixture()`
- Additive → `createValidSnapshotBundleFixture()` + identity of `fixtureMetadata`
- Request success → `createValidSnapshotBundleFixture()`
- Malformed success → fresh valid fixture; invalidate `catalog` to `[]`; retain
  existing privacy `leak` marker assertion carrier (not a vocabulary fixture
  string)

Counts remain 13 boundary / 16 request.

## 16. Fixture safety test

F5 collects recursive string values from valid+invalid fixtures; asserts closed
vocabulary, ≤ 4 KiB serialized, no `@`, `://`, secret-like, phone-like, ISO
date, UUID-like. Does not log serialized fixture data.

## 17. Controlled regressions

Fixed fixture SHA before probes:
`473fc085f6607cb9aadcdc321cbb5ba00d191b4739c00ae3e0c8aa3bb08d3c0a`

| Probe | Exit | Failing contract | Restored SHA |
|---|---:|---|---|
| R1 catalog→`[]` in V1 | 1 | F1 valid nullable rejected (`SnapshotBundleValidationError`); also F4 deep-equal side effect — not safety-only | match fixed |
| R2 `missing-catalog` gains catalog object | 1 | F3 invalid table; unreachable/`missing-catalog` path in failure evidence | match fixed |
| R3 email marker in additive metadata | 1 | F5 vocabulary / `@` safety | match fixed |

Post-probe: fixture 5/5, boundary 13/13, request 16/16.

## 18. Acceptance matrix

All A1–A105: **PASS**. No `PARTIAL`.

## 19. Validation log

### Pre-task gates (Node 22.23.1)

| Gate | Result |
|---|---|
| `npm ls --all` | exit 0 |
| boundary | **13/13** |
| request | **16/16** |
| full | **3 files / 30** |
| lint | exit 0 |
| typecheck | exit 0 |
| `make secret-scan` | PASS; node=22.23.1; files=1169 |

### Post-implementation

| Step | Result |
|---|---|
| V1 structure/size | fixture 148 lines / 3813 bytes; type-only import; authorized paths only |
| V2 fixture | **5/5** |
| V3 boundary | **13/13** |
| V4 request | **16/16** |
| V5 R1/R2/R3 | all nonzero; restored; post 5+13+16 |
| V6 full | **4 files / 35** |
| V7 lint | 0 |
| V8 typecheck | 0 / no emit |
| V9 build | 0; dist appeared (`PA-BUILD-001`) |
| V10 cleanup | dist removed; absent |
| V11 `npm ls` | 0; package/lock unchanged |
| V12 final suites | fixture 5; boundary 13; request 16; full 35; lint 0; typecheck 0 |
| V13 secret-scan | PASS; node=22.23.1; files=1171; findings 0 |
| V14 git | HEAD/branch unchanged; index empty |

## 20. Failure history

| Event | Outcome |
|---|---|
| Initial shell Node 24 / bare `fnm` missing on PATH | STOP risk mitigated by activating WinGet fnm → Node 22.23.1 before gates/writes |
| typecheck: `Buffer` in F5 | fixed via `TextEncoder` (no `@types/node`) |
| R1/R2/R3 intentional failures | expected nonzero; restored |
| Product gate failures after restore | none |

## 21. Source invariants

Unchanged SHA-256 (byte equality vs M1-04 freeze / recorded baselines):

| Path | SHA-256 |
|---|---|
| `apps/admin/src/api.ts` | `8dc0e8216e8b9062068bddccc2f13e6c97bd943bf8d1a2a15d4c3d69e142edc3` |
| `apps/admin/src/snapshot-boundary.ts` | `8a092ad58e765115fe0239aee3a6a443276d2e44c8f934ff977064851b2a8b27` |
| `apps/admin/src/App.tsx` | `40e3a98ed1e02503cdc3bd7e99c51ba747d8db3792cdb7c999f4f875f218af25` |
| `apps/admin/src/api.smoke.test.ts` | `72cd0b617ff2e8222bab06baf0a0ca0ebe4b2caf16d26d008a8d71c2e6b080d8` |
| `apps/admin/package.json` | `fa56ff8dafd751aad4f369a94ee1c02058b60c68dcb0bb37d18861d3c32951d2` |
| `apps/admin/package-lock.json` | `eca2158c76baf43739f433f7b1e538d044464e426e6e5a5149a01599ada53577` |
| `docs/admin-cms/execplans/M1-04-R0.md` | `4961b25142ba3824a0f044a470545c508fa1d91f4ab0a65f3271e621331ec579` |
| governance / workflows / web / generated | unchanged by this task |

Modified tests: 13 / 16 `it(` names retained; assertions not weakened; only
fixture construction/reuse changed.

## 22. Build cleanup

`PA-BUILD-001`: build created `apps/admin/dist/**`; removed immediately after;
verified absent at freeze. No dist staged.

## 23. Freeze

Primary artifacts (ExecPlan / dist / node_modules excluded):

| Path | SHA-256 |
|---|---|
| `apps/admin/src/test-fixtures/snapshot-bundle.fixtures.ts` | `473fc085f6607cb9aadcdc321cbb5ba00d191b4739c00ae3e0c8aa3bb08d3c0a` |
| `apps/admin/src/snapshot-fixtures.test.ts` | `926122ab3a7da137aa4c9908d0c236f3bb2851b55cecaf62122bbbe6a4714ba8` |
| `apps/admin/src/snapshot-boundary.test.ts` | `9d06b3116e50ac48eb21ed0dcc0f9c7ffe5c8070adf2a304adc517172e1ad04f` |
| `apps/admin/src/api.request-contract.test.ts` | `7d57e39576848a3663621caa2de5007ab45b6e0d7d1d4707b7a0fea33917b13d` |

Algorithm: SHA-256 over sorted records `relative path` NUL `bytes` NUL.

**Primary fingerprint:** `e1a5e591efbf5419f8ed08054cc5f379f84abcb2da9a6ca9261e215e4f9355aa`

Reference:

- M1-04 primary: `26dc2a053f92173f416df51e528608a007d38b825f4e84aa228fc1209076d6d3`
- M1-04-R0 full-file: `4961b25142ba3824a0f044a470545c508fa1d91f4ab0a65f3271e621331ec579`

Serialized fixture data sample size: **1902** UTF-8 bytes (≤ 4096).

## 24. Review

Independent subagent unavailable; parent performed a separate read-only
frozen-synthetic-snapshot-fixture review.

Checked: synthetic/minimal factories; no production/generated sources; two
valid fresh factories; six invalid fresh fixtures; missing-vs-undefined;
closed vocabulary; no PII/secrets/URLs/credentials; bounded sizes; five
fixture tests; boundary/request counts+assertions preserved; fixtures reused;
no production source changes; R1–R3 fail+restore; gates/build cleanup/secret
scan; no M1-06/M1-09; fingerprint.

**Verdict:**

```text
APPROVE
```

## 25. Git actions

```text
Staged: NO
Committed: NO
Pushed: NO
```

## 26. Component classification

| Component | Classification | Rationale |
|---|---|---|
| `snapshot-bundle.fixtures.ts` | `repository_test_fixture` | test-only synthetic envelope factories |
| `snapshot-fixtures.test.ts` | `repository_test` | integrity/determinism/safety suite |
| `snapshot-boundary.test.ts` | `repository_test` | existing boundary suite with fixture reuse |
| `api.request-contract.test.ts` | `repository_test` | existing request suite with fixture reuse |
| `M1-05.md` | `repository_execution_record` | ExecPlan |

## 27. Final status

- Status: `DONE`
- Outcome: synthetic valid/invalid snapshot fixtures + integrity suite;
  narrow reuse in boundary/request tests; production parser/API unchanged;
  review APPROVE; no commit.
- M1-06 / M1-09: **not started**
