# WEB-AUDIT-POLISH-2026-09-24 — audit corrective delivery

## Metadata / authority / objective

Status: VERIFIED LOCAL CANDIDATE / RELEASE PENDING. Owner request: «исправь всё, отшлифуй сайт до идеала», following the 24 September production audit. This is the task packet and execution record for that corrective delivery, not a promise of defect-free software. Base `bbc7b274aa371b2d5dd0a9df2fe4b8e24d063290`; branch `fix/schedule-data-and-venues-2026-09-09`; repository `/home/xipsin/projects/BM_QuestHub/design-previews/schedule-audit-2026-09-09/repo`.

Implement and verify the audit corrections: eight explicit school2044 supersessions, honest capacity freshness and refresh path, coherent venue programme lists, contrast/landmarks, SEO/static error routing, useful initial HTML and bounded lead submission. Preserve reviewed teachers/levels, original history and old links.

## Non-goals / release boundary

No redesign/rebrand, dependency upgrades, schema/platform migration, credentials disclosure, unrelated services/publications changes, destructive cleanup or real lead submissions. NO COMMIT, staging, push, merge or production execution in this local implementation phase. Deployment and scheduler activation require a release decision and exact operational verification; do not call local code a live fix.

## Paths / protected overrides

Ordinary source changes: `apps/web/content/annual-retirements.mjs`; `apps/web/lib/offers/{course-finder,schedule-board,mos-availability,use-live-schedule}.ts`; relevant offer tests; `apps/web/lib/lead-submit-client.ts`; related submission tests; `apps/web/lib/sites/*`; `apps/web/components/{course-finder,course-finder-results,schedule-capacity-indicator,metro-label,live-agenda}.tsx`; capacity CSS; `apps/web/app/globals.css`; `apps/web/app/quests/[slug]/page.tsx`; `apps/web/app/sites/[school]/page.tsx`; `apps/web/app/catalog/page.tsx`; new public robots/sitemap/not-found source and regression tests; `scripts/integrate-year-schedule.ts`; targeted verification scripts/tests; this plan and delivery report.

PA-GEN-001 / PA-COMPAT-003: generator-only write to `apps/web/data/offers-snapshot.json` via `scripts/integrate-year-schedule.ts --write --tier=hot`; preserve schema, source IDs and unrelated offers; `--check --tier=hot`, snapshot validation, integration tests and exact diff review required. Rollback via generator/input revision, not deletion. No manual generated edits.

PA-COMPAT-007: compatible public UI improvements and explicit old-offer aliases; user-approved old/new pairs only. Existing private MAX and PII remain absent from public artifacts.

PA-BUILD-002/003: web build outputs and Next caches may be generated by validation, never staged. Dedicated test output/report folders are task-owned. Existing 65 untracked files remain untouched; inspect overlapping operational helpers read-only first. Any additional protected write must be recorded with exact path and operation before execution; cloud execution remains separate.

## Preflight

2026-09-24 MSK: pwd/root/branch/HEAD/log/status inspected, tracked tree and index clean, 65 pre-existing untracked paths retained; `git diff --check` exit0. Node v22.23.1/npm10.9.8 via `/home/xipsin/.nvm/versions/node/v22.23.1/bin`. Root AGENTS, PLANS, protected registry and ExecPlan template read. No package install required.

## Architecture / classification / audit A and B

All changes are product_domain: source groups → reviewed editorial overrides/identity lifecycle → validated snapshots → live merge → selectors → UI. Public compatibility stays additive; no framework/database imports into pure domain. Auditors Hume (refresh/hosting path) and Ramanujan (failure boundaries/SSR) are independent read-only agents; parent alone edits. Prior audit report and source findings are inputs, not evidence of completed corrections.

## UX contract

Parent selects school/address → programme/year → day/time → verified direct mos.ru link. Old group links resolve only through explicit confirmed mappings, never guessed by slot. Stale capacity is dated and neutral, not a current green claim; page-level error remains actionable. Current/archived programmes must agree with counts. Keep optional form, 15-second controllable transition, keyboard/focus and narrow reflow. References: audited production flows and `docs/admin-cms/01_ux/18_ACCESSIBILITY_AND_RESPONSIVE.md` (where applicable to public UI).

## Security / failure modes

No production secrets needed for local corrections. MOS manual verification uses bounded public GET and exact identity binding, no registrations. Do not infer cancellations from timeout/full capacity. Raw responses/PII never enter diagnostics. Lead retry must not claim failure after known partial success or invent durable idempotency without storage. Network checks are operational evidence, not deterministic test dependencies.

## Implementation / tests

1. Explicit supersession mapping, consistent archive/current selectors, deep-link compatibility; regression fixtures for all8 pairs, К4045, different campuses and multi-day875.
2. Refresh inspection, local direct-card verification, stale-state UX and failure recovery; test data binding, timeout and unchanged editorial fields.
3. Coherent venue content, contrast and main landmark; SEO files/404 delivery path and initial rendered content.
4. Bounded lead submission and explicit partial delivery contract if feasible without unapproved infrastructure.
5. Targeted Node22 tests, `npx tsc --noEmit` in web, `npm --prefix apps/web run check` with explicit local data configuration, admin build where applicable; browser desktop/mobile, axe, navigation/aliases and no real submissions. Freeze and independent review after validation.

Additional exact commands permitted for this corrective task: `node --import ./apps/web/node_modules/tsx/dist/loader.mjs --test <explicit changed test files>` or existing equivalent tsx runner; `node --test <explicit mjs tests>`; `node --import ... scripts/integrate-year-schedule.ts --write --tier=hot` and `--check --tier=hot`; existing public-MOS read-only verifier; task-specific Playwright scripts with intercepted writes; `git diff --check`. No arbitrary deploy commands implied.

## Acceptance

| Criterion | Status |
|---|---|
| 2044 current11/global62, retained history and explicit aliases | PASS unit + browser scenarios |
| Honest dated capacity and bounded refresh behavior | PASS local; production scheduler NOT activated |
| Counts/programmes, contrast, landmarks corrected | PASS source/unit + final33-viewport scan |
| SEO/404 locally verified; production boundary documented | PASS local static export; host fallback NOT changed |
| Lead failure paths bounded without fabricated delivery claims | PASS transport + intercepted browser; durable backend idempotency deferred |
| Unit/type/build/browser validation and independent review | 107 web tests,19 MOS tests,38 receiver tests,build/type PASS;33browser scans/11scenario areas PASS;A+B APPROVE frozenhash |

## Validation / failure history

All commands use existing Node22 PATH. Initial shell had no Node; inspection typo `scripts/lib/mos-annual-lifecycle.mjs` absent; corrected by discovery. Root tsx-loader command failed alias resolution; corrected to run from `apps/web`. No data writes from these failures.

- Generator `--write --tier=hot` changed exactly eight `isArchived:false→true` flags, retaining all99 offers/74 annual history. Backup `/tmp/questhub-year-data-backup-clvlEB`; later `--check --tier=hot` PASS. Current62 groups/63 weekly rows.
- Initial unit failures asserted old19/70 counts. Updated only changed contract expectations, with new explicit8-pair/lifecycle/alias regressions. Existing optional fixture types corrected via schema parse/assert. Full web offer+transport run `/tmp/questhub-polish-unit5.log`:107 PASS,0 fail. TypeScript PASS.
- First lint run flagged mounted-ref usage through render-time handler creation; event wrapper fixed it. Latest full `npm run check` `/tmp/questhub-polish-check5.log`:PASS with explicit `SITE_SNAPSHOT_SOURCE=file OFFERS_SNAPSHOT_SOURCE=file NEXT_PUBLIC_SITE_URL=https://b-master.pro NEXT_PUBLIC_OFFERS_SNAPSHOT_URL=/data/offers-snapshot.json`. 122-page static export. Four existing lint warnings and pre-existing broad file-tracing warning remain; S3 verification deliberately skipped for local file mode, not called production proof.
- MOS suites initially failed old4-only inactive assertion and missing Node `--experimental-test-module-mocks`; corrected12 assertion, rerun with required flag. `/tmp/questhub-polish-mos-tests-final.log`:19 PASS. Receiver mocked tests `/tmp/questhub-polish-receiver-tests.log`:38 PASS. No real submissions.
- Browser rounds1–3 retained under `/tmp/questhub-polish-browser-{tvNFMY,FvJ4w6,aejqDF}`: found invalid role on new notice, nested complementary blocks, catalogue heading gaps; corrected source. Harness also incorrectly expected a school query in a deliberately shortened school-path share URL, wrong Wednesday count (actual5), missing `year:` ID prefix, and counted diagnostic POST as lead POST; assertions corrected to actual contracts, never removed. Round1 independent audits found durable-review error lost after timeout, quest CTA not ageing, HTTP error diagnostics lost, and insufficient scenario assertions; each corrected and regression-covered.
- Scenario-only run `/tmp/questhub-polish-browser-JdCJUd/report.json`:all checks PASS including history/new-tab share,8selected aliases+quest alias,15s popup/escape/focus, intercepted502 and success,35s timeout, no late success/reset after form reopen, synthetic capacity/staleness/identity block, clock ageing, no-JS actual62/11 groups, robots/sitemap/canonical and local404. Finalfull run `/tmp/questhub-polish-browser-FdfqWS/report.json`:33scans,11scenario areas,0failures on same frozen bytes. No detected axe violations,JSerrors,overflow or broken loaded images in those scans; not a complete accessibility certification.
- Real read-only MOS run:59/62 verified, one transient timeout, one boundary-title space difference, one genuine school875 schedule change. Corrected harmless whitespace validation; targeted repeat `/tmp/questhub-polish-mos-targeted-final.json` verified2/2 (К4609 andК2372). This is not a claim that the original batch was61/62 or that automatic production sync works. School875 source changes await owner confirmation; no guessed override applied.

## Scope changes

Browser audit additions within public compatibility: `apps/web/components/{live-catalog,catalog-page-client,offer-booking-action}.tsx` to expose the correct page heading and use the action's computed CTA label; `scripts/lib/annual-retirements.test.mjs` to assert all12 inactive records rather than the former4. Existing untracked tests are not edited. These correct observed regression/consistency issues, not a redesign.

Independent-review corrections: pure `retainMosReviewError` in `apps/web/lib/offers/annual-schedule.ts`, used by tracked capacity runtime and client reconciliation for the same exact binding. No schema changes. Fresh success can clear review; transient failure cannot. ScheduleBoard clock dependency, safe typed HTTP diagnostics and stream cancellation, stronger synthetic/browser/no-JS assertions. All within audit corrective scope.

Within the owner-approved audit corrective scope: new pure `lead-delivery` transport and tests, `annual-freshness` and shared clock, `static-course-content` fallback, home/school catalog fallbacks and booking mounted guard. Targeted tracked `scripts/lib/mos-live-capacity.ts` title comparison trims boundary whitespace only, with a new regression test. The pre-existing untracked operational handler and its tests remain untouched; invocation budget/activation are a release prerequisite, not a local success claim. New task-owned `scripts/verify-site-audit-polish.mjs` verifies exported HTML and browser behavior with all writes intercepted. No cloud writes or protected scope expansion.

## Freeze / independent review / delivery

Frozen42 source/test/generated files fingerprint `42debaed827f86defb8fe6eb2553a8b83445e8423edc293af716b5669170ce65`. Algorithm SHA256(sorted relative path + NUL + bytes + NUL), plan/report excluded from self-reference. Manifest `/home/xipsin/projects/BM_QuestHub/reports/site-polish-2026-09-24/candidate-manifest.json`. Independent auditors Hume(A) and Ramanujan(B) both reverified all42hashes and APPROVED within their scopes; no remainingP0–P2 in reviewed local candidate. Staging/commit/push: NOT AUTHORIZED. No live changes claimed. Delivery report `/home/xipsin/projects/BM_QuestHub/reports/site-polish-2026-09-24/REPORT.md`; screenshots and JSON evidence copied beside it. Sourcehashes unchanged after finalreview; operational helper/test baseline hashes unchanged.

Release prerequisites remain explicit: fix/bound existing untracked cloud handler's invocation budget before activation; inspect existing controller/YMQ responsibilities and exact two ops keys, then validate controlled five-minute runs. Actual Timeweb fallback must be changed/verified at the host; local404.html is insufficient. Durable receiver idempotency/partial-delivery recovery requires an approved persistent journal/protocol; frontend changes only bound uncertainty and prevent automatic retries. Do not silently include unrelated65 baseline untracked files or `services/` in a release.
