# Secretlint exclusions for BM QuestHub.
# Every active entry requires an SS-EXC record in docs/admin-cms/SECRET_SCAN.md.
# Broad repository, docs, source, script, snapshot, lockfile, or config globs are forbidden.
