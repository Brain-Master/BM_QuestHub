# Timeweb cold deploy (required for make content-publish)
TIMEWEB_API_TOKEN=
TIMEWEB_APP_ID=

# S3 upload uses scripts/s3.env (see scripts/s3.env.example)

# Apps Script → POST /sync/* on content-admin → GitHub sheet-sync.yml
CONTENT_REBUILD_GITHUB_TOKEN=
CONTENT_REBUILD_REPOSITORY=Brain-Master/BM_QuestHub
CONTENT_REBUILD_REF=main
